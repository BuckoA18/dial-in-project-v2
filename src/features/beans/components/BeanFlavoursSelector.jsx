const BeanFlavoursSelector = ({ children }) => {
  return (
    <div className="flex max-h-120 flex-col gap-2 overflow-scroll sm:gap-2">
      {children}
    </div>
  );
};

export default BeanFlavoursSelector;
